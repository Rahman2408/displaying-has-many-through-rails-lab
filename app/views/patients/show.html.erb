
<% @patient.appointments.each do |appt| %>
       
        When: <%= appt.appointment_datetime.to_date.to_s(:long) %> at <%= appt.appointment_datetime.strftime("%H:%M")  %><br>
        Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor.id) %>
    <% end%>