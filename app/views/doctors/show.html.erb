
<% @doctor.appointments.each do |appt| %>
       
        When: <%= appt.appointment_datetime.to_date.to_s(:long) %> at <%= appt.appointment_datetime.strftime("%H:%M")  %><br>
        Patient: <%= link_to appt.patient.name, patient_path(appt.patient.id) %>
    <% end%>